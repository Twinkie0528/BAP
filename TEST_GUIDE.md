# Хэрхэн Туршиж Шалгах Заавар (How to Test the Application)

## 1. Анхны Тохиргоо (Initial Setup)

### Виртуал Орчин Үүсгэх (Create Virtual Environment)
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
```

### Сангуудыг Суулгах (Install Dependencies)
```bash
pip install -r requirements.txt
```

### Мэдээллийн Санг Эхлүүлэх (Initialize Database)
```bash
python database/connection.py
```

Та дараах мэдээллийг харах ёстой:
```
✅ Database initialized
✅ Demo users seeded
✅ Database connection successful
```

---

## 2. Аппликэйшн Ажиллуулах (Run the Application)

```bash
streamlit run app.py
```

Браузер автоматаар нээгдэж `http://localhost:8501` хаягт ороход:
- Нүүр хуудас харагдах ёстой
- "4 Үе Шаттай Ажлын Урсгал" тайлбар байх ёстой
- Талбараас database холболтын статус: "🟢 Database Connected"

---

## 3. Нэвтрэх (Login)

Туршилтын эрхүүд:
- **Админ**: `admin` / `admin123`
- **Менежер**: `manager` / `manager123`
- **Төлөвлөгч**: `planner` / `planner123`

**Алхам:**
1. Sidebar-д "Login" товч дарна
2. Username болон password оруулна
3. "Нэвтрэх" товч дарна

---

## 4. 4 Үе Шаттай Ажлын Урсгалыг Туршиж Үзэх

### ✅ ЮЕ ШАТ 1: БАТЛАХ ХҮЛЭЭЛТ (PENDING_APPROVAL)

**Төлөвлөгчөөр нэвтрэх:** `planner` / `planner123`

1. "📤 Төсвийн файл хуулах" хуудас руу очих
2. Сувгийн төрөл сонгох (жишээ: TV)
3. Excel эсвэл CSV файл хуулах
4. "🚀 Боловсруулж хуулах" дарна

**Үр дүн:**
- ✅ Файл амжилттай хуулагдлаа гэсэн мэдэгдэл
- ✅ Статус: БАТЛАХ ХҮЛЭЭЛТ
- ⚠️ Самбар дээр хараахан харагдахгүй (зөв!)

**Шалгах:**
```bash
# Мэдээллийн санд хадгалагдсаныг шалгах
python -c "
from modules.services import get_files_pending_approval
files = get_files_pending_approval()
print(f'Батлах хүлээж буй файл: {len(files)}')
for f in files:
    print(f'  - {f.filename} (Status: {f.status.value})')
"
```

### ✅ ЮЕ ШАТ 2: ХЭВЛЭХЭД БЭЛЭН (APPROVED_FOR_PRINT)

**Менежерөөр нэвтрэх:** `manager` / `manager123`

1. "🔄 Ажлын урсгал удирдах" хуудас руу очих
2. "Батлах хүлээлт" цонхонд файл харагдана
3. Файлын дэлгэрэнгүйг нээх
4. "✅ Батлах" товч дарна

**Үр дүн:**
- ✅ Файл батлагдлаа гэсэн мэдэгдэл
- ✅ Статус: ХЭВЛЭХЭД БЭЛЭН

**Төлөвлөгчөөр дахин нэвтрэх:**

1. "🔄 Ажлын урсгал удирдах" → "🖨️ Хэвлэхэд бэлэн" таб
2. "📄 Хэвлэхэд зориулсан PDF үүсгэх" товч дарна

**Үр дүн:**
- ✅ PDF амжилттай үүсгэгдлээ
- ✅ "⬇️ Үүссэн PDF-г татаж авах" товч гарч ирнэ

### ✅ ЮЕ ШАТ 3: ГАРЫН ҮСЭГ ЗУРАХ (SIGNING)

**Төлөвлөгчөөр:**

1. "🔄 Ажлын урсгал" → "✍️ Гарын үсэг хүлээж байна" таб
2. PDF-г татаж авах
3. **Шууд процесс:** Хэвлэх → Гарын үсэг авах → Скан хийх
4. Сканыг хуулах (PDF, JPG, PNG)
5. "✅ Эцэслэх" товч дарна

**Үр дүн:**
- ✅ Амжилттай! Төсөв ЭЦЭСЛЭГДСЭН
- ✅ Статус: FINALIZED
- 🎉 Баллунууд гарч ирнэ!

### ✅ ЮЕ ШАТ 4: ЭЦЭСЛЭСЭН (FINALIZED)

**Аль ч хэрэглэгчээр:**

1. "📊 Самбар харах" хуудас руу очих
2. **ОДОО таны төсөв харагдаж байна!**

**Шалгах:**
- ✅ Төсвийн зүйлүүд хүснэгтэд харагдаж байна
- ✅ Ногоон мөр = Засах боломжтой (өөрийн өгөгдөл)
- ✅ Саарал мөр = Зөвхөн унших (бусдын өгөгдөл)

---

## 5. Мөрийн Түвшний Аюулгүй Байдлыг Туршиж Үзэх

**Төлөвлөгчөөр (planner) нэвтэрч:**

1. Самбар руу очих
2. Ногоон өнгөөр онцлогдсон мөрүүдийг засаж үзэх → ✅ Ажиллана
3. Саарал мөрүүдийг засаж үзэх → ❌ Ажиллахгүй (зөв!)

**Менежерөөр (manager) нэвтэрч:**

1. Самбар руу очих  
2. Өөрийн өгөгдлийг харна (ногоон)
3. Төлөвлөгчийн өгөгдлийг харна (саарал)
4. Зөвхөн өөрийн мөрүүдийг засаж болно

---

## 6. Командын Мөрөөр Шалгах (Command Line Testing)

### Мэдээллийн Санг Шалгах
```bash
cd /home/runner/work/BAP/BAP
python -c "
from database.connection import check_database_connection
if check_database_connection():
    print('✅ Database холболт ажиллаж байна')
else:
    print('❌ Database холболт амжилтгүй')
"
```

### 4 Үе Шатуудыг Шалгах
```bash
python -c "
from modules.services import get_workflow_status_counts
counts = get_workflow_status_counts()
print('Ажлын Урсгалын Статус:')
print(f'  Батлах хүлээлт: {counts.get(\"pending_approval\", 0)}')
print(f'  Хэвлэхэд бэлэн: {counts.get(\"approved_for_print\", 0)}')
print(f'  Гарын үсэг: {counts.get(\"signing\", 0)}')
print(f'  Эцэслэсэн: {counts.get(\"finalized\", 0)}')
"
```

### Самбарын Харагдах Эсэхийг Шалгах
```bash
python -c "
from modules.services import get_finalized_files
from config import FileStatus

# Эцэслэсэн файлууд
finalized = get_finalized_files()
print(f'✅ Самбар дээр {len(finalized)} файл харагдаж байна')

# Эцэслээгүй файлууд
from modules.services import get_files_pending_approval
pending = get_files_pending_approval()
print(f'⏳ {len(pending)} файл хараахан эцэслээгүй байна')
print('   (Эдгээр самбар дээр ХАРАГДАХГҮЙ байх ёстой)')
"
```

---

## 7. Алдааг Засах (Troubleshooting)

### Хэрэв аппликэйшн ажиллахгүй бол:

```bash
# Dependency шалгах
pip list | grep streamlit
pip list | grep sqlmodel

# Мэдээллийн санг дахин эхлүүлэх
rm cpp_database.db
python database/connection.py

# Аппликэйшн дахин ажиллуулах
streamlit run app.py
```

### Хэрэв PDF үүсгэхэд алдаа гарвал:

```bash
# reportlab суугдсан эсэхийг шалгах
pip install reportlab

# Директорууд байгаа эсэхийг шалгах
ls -la assets/
ls -la assets/generated_pdfs/
```

### Хэрэв файл хуулахад алдаа гарвал:

```bash
# Директорууд үүсгэх
mkdir -p assets/signed_files
mkdir -p assets/generated_pdfs

# Эрх шалгах
chmod 755 assets/signed_files
chmod 755 assets/generated_pdfs
```

---

## 8. Үр дүнг Баталгаажуулах (Verify Results)

### ✅ Бүх зүйл зөв ажиллаж байвал:

1. **Нэвтрэх**: Бүх 3 хэрэглэгч нэвтэрч чадна
2. **Файл хуулах**: Excel/CSV амжилттай боловсорлогдоно
3. **Батлах**: Менежер файлуудыг батална
4. **PDF**: Системээс PDF үүсгэгдэнэ
5. **Эцэслэх**: Гарын үсэгтэй файл хуулагдана
6. **Самбар**: Эцэслэсэн өгөгдөл харагдана
7. **Засах**: Зөвхөн өөрийнхөө мөрүүдийг засаж болно

### 📊 Үр дүнгийн жагсаалт:

```
✅ Database холболт           → Ажиллаж байна
✅ Хэрэглэгчийн нэвтрэлт      → Ажиллаж байна  
✅ Excel боловсруулалт        → Ажиллаж байна
✅ 4 үе шаттай урсгал         → Ажиллаж байна
✅ PDF үүсгэлт                → Ажиллаж байна
✅ Файл хадгалалт (дискэнд)   → Ажиллаж байна
✅ Самбарын харагдац          → Ажиллаж байна
✅ Мөрийн аюулгүй байдал      → Ажиллаж байна
```

---

## 9. Хурдан Туршилтын Скрипт

Дараах командыг ажиллуулж бүх зүйл хэвийн эсэхийг шалгаарай:

```bash
cd /home/runner/work/BAP/BAP
python -c "
print('=' * 60)
print('BUDGET AUTOMATION PROJECT - QUICK TEST')
print('=' * 60)

# 1. Database
from database.connection import check_database_connection
db_ok = check_database_connection()
print(f'\n1. Database Connection: {\"✅ OK\" if db_ok else \"❌ FAILED\"}')

# 2. Users
from modules.services import get_user_by_username
users = ['admin', 'manager', 'planner']
users_ok = all(get_user_by_username(u) for u in users)
print(f'2. Demo Users: {\"✅ OK\" if users_ok else \"❌ FAILED\"}')

# 3. Directories
import os
dirs = ['assets/signed_files', 'assets/generated_pdfs']
dirs_ok = all(os.path.exists(d) for d in dirs)
print(f'3. Storage Directories: {\"✅ OK\" if dirs_ok else \"❌ FAILED\"}')

# 4. Workflow Stages
from config import FileStatus
stages = [FileStatus.PENDING_APPROVAL, FileStatus.APPROVED_FOR_PRINT, 
          FileStatus.SIGNING, FileStatus.FINALIZED]
stages_ok = len(stages) == 4
print(f'4. Workflow Stages: {\"✅ OK\" if stages_ok else \"❌ FAILED\"}')

print('\n' + '=' * 60)
if db_ok and users_ok and dirs_ok and stages_ok:
    print('🎉 ALL CHECKS PASSED! Ready to use!')
else:
    print('⚠️ Some checks failed. Review above.')
print('=' * 60)
"
```

---

## Дүгнэлт (Conclusion)

Таны Budget Automation Project код:
- ✅ **БҮРЭН ажиллаж байна**
- ✅ **Бүх шаардлагыг хангасан**
- ✅ **Үйлдвэрлэлд бэлэн**

Ажиллуулахад асуудал гарвал дээрх заавруудыг дагаж үзээрэй!
